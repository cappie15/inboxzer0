{
  "name": "inboxzer0-server",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted backend for InboxZer0: handles IMAP connectivity, credential storage and AI drafting so the web frontend never needs raw sockets or API keys in the browser.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
